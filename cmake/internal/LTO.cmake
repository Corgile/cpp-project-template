IF(${PROJECT_NAME}_ENABLE_LTO)
  INCLUDE(CheckIPOSupported)
  CHECK_IPO_SUPPORTED(RESULT result OUTPUT output)
  IF(result)
    SET(CMAKE_INTERPROCEDURAL_OPTIMIZATION TRUE)
  ELSE()
    MESSAGE(SEND_ERROR "IPO 不支持: ${output}.")
  ENDIF()
ENDIF()
